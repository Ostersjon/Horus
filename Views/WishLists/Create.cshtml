@model Castle.Models.WishList

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>WishList</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IuserId" class="control-label"></label>
                <select asp-for="IuserId" class ="form-control" asp-items="ViewBag.IuserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="MediaId" class="control-label"></label>
                <select asp-for="MediaId" class ="form-control" asp-items="ViewBag.MediaId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}